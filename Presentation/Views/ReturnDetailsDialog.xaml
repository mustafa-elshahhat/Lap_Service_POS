<Window x:Class="AlJohary.ServiceHub.Presentation.Views.ReturnDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تفاصيل المرتجع"
        Width="650" Height="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FlowDirection="RightToLeft"
        KeyDown="Window_KeyDown">

    <Border Style="{DynamicResource StandardWindowContentBorder}">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Button Style="{DynamicResource CloseButton}" 
                        HorizontalAlignment="Right" VerticalAlignment="Top"
                        Click="CloseButton_Click"/>
                
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="🔁 تفاصيل المرتجع" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryBrush}"/>
                    <TextBlock x:Name="ReturnNumberText" Text="رقم المرتجع: -" FontSize="12" Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                
                <Grid Grid.Row="0" Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="{DynamicResource LightBrush}" Padding="12" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="الفاتورة الأصلية" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="InvoiceNumberText" Text="-" FontWeight="Bold" FontSize="14"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Column="2" Background="{DynamicResource LightBrush}" Padding="12" CornerRadius="8">
                        <StackPanel>
                            <TextBlock Text="تاريخ الارتجاع" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="ReturnDateText" Text="-" FontWeight="Bold" FontSize="14"/>
                        </StackPanel>
                    </Border>
                </Grid>

                
                <DataGrid x:Name="ItemsGrid" Grid.Row="1" Style="{DynamicResource DialogDataGrid}" IsReadOnly="True" FlowDirection="LeftToRight">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="المنتج" Binding="{Binding [product_name]}" Width="*"/>
                        <DataGridTextColumn Header="الكمية" Binding="{Binding [quantity]}" Width="80">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="السعر" Binding="{Binding [unit_price], StringFormat=N2}" Width="100">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="الإجمالي" Binding="{Binding [total_price], StringFormat=N2}" Width="100">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                
                <Border Grid.Row="2" Margin="0,16,0,0" Padding="16" Background="{DynamicResource DangerLighterBrush}" CornerRadius="8" BorderBrush="{DynamicResource DangerLightBrush}" BorderThickness="1">
                     <Grid>
                         <TextBlock Text="إجمالي المبلغ المسترد" FontWeight="SemiBold" Foreground="{DynamicResource DangerDarkBrush}" VerticalAlignment="Center"/>
                         <TextBlock x:Name="TotalAmountText" Text="0.00" HorizontalAlignment="Right" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}"/>
                     </Grid>
                </Border>
            </Grid>

            
            <StackPanel Grid.Row="2" Margin="0,24,0,0" HorizontalAlignment="Center" Orientation="Horizontal">
                <Button Content="🖨️ طباعة المرتجع" Style="{DynamicResource PrimaryButton}" Width="140" Margin="10,0" Click="PrintButton_Click"/>
                <Button Content="إغلاق" Style="{DynamicResource DarkButton}" Width="120" IsCancel="True" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
