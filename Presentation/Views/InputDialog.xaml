<Window x:Class="AlJohary.ServiceHub.Presentation.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:AlJohary.ServiceHub.Presentation.Helpers"
        Title="إدخال"
        helpers:DialogChrome.DimOwner="True"
        SizeToContent="WidthAndHeight"
        MinWidth="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        FlowDirection="RightToLeft">

    <Window.Resources>
        <Storyboard x:Key="OnLoadedStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.8" To="1.0" Duration="0:0:0.2" DecelerationRatio="0.5"/>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.8" To="1.0" Duration="0:0:0.2" DecelerationRatio="0.5"/>
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource OnLoadedStoryboard}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border x:Name="MainBorder" 
            Style="{DynamicResource StandardWindowContentBorder}"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <ScaleTransform ScaleX="1" ScaleY="1"/>
        </Border.RenderTransform>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> 
                <RowDefinition Height="Auto"/> 
                <RowDefinition Height="Auto"/> 
                <RowDefinition Height="Auto"/> 
            </Grid.RowDefinitions>

            
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock x:Name="TitleLabel"
                           Text="تعديل"
                           FontSize="20"
                           FontWeight="Bold"
                           FontFamily="{DynamicResource MainFontSemibold}"
                           Foreground="{DynamicResource PrimaryBrush}"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>
            </StackPanel>

            
            <TextBlock x:Name="PromptLabel"
                       Grid.Row="1"
                       Text="الرجاء إدخال القيمة الجديدة:"
                       FontSize="13"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,0,0,24"/>

            
            <TextBox x:Name="AnswerTextBox"
                     Grid.Row="2"
                     Style="{DynamicResource MainTextBox}"
                     Height="50"
                     FontSize="18"
                     FontWeight="Bold"
                     TextAlignment="Center"
                     Margin="0,0,0,32"/>

            
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center">
                
                <Button x:Name="OkButton"
                        Content="تأكيد التعديل"
                        Style="{DynamicResource PrimaryButton}"
                        MinWidth="160"
                        Height="48"
                        Margin="8,0"
                        IsDefault="True"
                        Click="OkButton_Click"/>

                <Button x:Name="CancelButton"
                        Content="إلغاء"
                        Style="{DynamicResource DarkButton}"
                        MinWidth="120"
                        Height="48"
                        Margin="8,0"
                        IsCancel="True"
                        Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
